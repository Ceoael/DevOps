apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: my-nginx-replicaset
  labels:
    component: web
    app: my-app-1

spec:
  template:
    metadata:
      name: my-nginx-pod
      labels:
        component: web
        app: my-app-1
    spec:
      containers:
        - name: my-nginx-container
          image: ceoael/mynginx
  replicas: 5
  selector:
    matchLabels:
      app: my-app-1
      component: web